setw -g mode-keys vi                                                                                         
# Move among windows
bind k selectp -U
bind j selectp -D
bind h selectp -L
bind l selectp -R

set-option -g default-terminal "rxvt-256color"
